# URL del Traccar para el build de producción (Cloudflare Pages la usa en `npm run build`).
# No es secreto: es el endpoint público del servidor. El login (user/pass) va en runtime.
VITE_TRACCAR_URL=https://traccar.zonamaker.com.ar
